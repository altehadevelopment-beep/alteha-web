// Detalle de subasta de la clínica: reutiliza el módulo completo del médico
// (misma vista y forma de enviar la oferta). El rol (CLINIC), los enlaces y la
// liquidación se resuelven por la ruta.
export { default } from '../../../specialist/auctions/[id]/page';
